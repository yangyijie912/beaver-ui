name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  unit-tests:
    name: Unit Tests (Node)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Enable corepack
        run: corepack enable && corepack prepare pnpm@latest --activate
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run unit tests (node env)
        # vitest CLI does not accept `--env`; use config defaults (node) or `--environment`.
          run: pnpm test -- --run

  e2e:
    name: E2E / Browser Tests + Build
    needs: unit-tests
    runs-on: ubuntu-latest
    env:
      PLAYWRIGHT_BROWSERS_PATH: '~/.cache/ms-playwright'
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Enable corepack
        run: corepack enable && corepack prepare pnpm@latest --activate
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Ensure Playwright cache dir
        run: mkdir -p ~/.cache/ms-playwright

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: "${{ runner.os }}-playwright-browsers-${{ hashFiles('**/pnpm-lock.yaml') }}"
          restore-keys: |
          restore-keys: |
            ${{ runner.os }}-playwright-browsers-
      - name: Install Playwright browsers
        env:
          PLAYWRIGHT_BROWSERS_PATH: '~/.cache/ms-playwright'
        run: pnpm dlx playwright install --with-deps
      - name: Debug: list Playwright cache
        run: ls -la ~/.cache/ms-playwright || true
      - name: Run browser tests
        run: pnpm test -- -c vitest.browser.config.ts --run
      - name: Build
        run: pnpm run build
